trigger: none

resources:
  repositories:
  - repository: templates
    type: git
    name: DevopsTools/prodest-build-deploy-templates

  pipelines:
  - pipeline: buildPipeline
    source: esesp-trilhas-build
    trigger: none
    version: SelecionarBuild

stages:
- stage: Deploy_DEV
  jobs:
  - template: deployDotnetGitopsV2_1.yml@templates
    parameters:
      nomeApp: trilhas-web
      namespace: esesp-trilhaaprendizagem
      ambiente: dev
      library: esesp-trilhas-dev
      repoGitops: esesp-trilhaaprendizagem-gitops
      caminhoAppSettings: Trilhas