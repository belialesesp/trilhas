trigger: none

resources:
  repositories:
  - repository: templates
    type: git
    name: DevopsTools/prodest-build-deploy-templates

  pipelines:
  - pipeline: buildPipeline
    source: esesp-trilhas-build
    trigger: none
    version: SelecionarBuild

stages:
- stage: Deploy_hom
  jobs:
  - template: deployDotnetGitopsV2.yml@templates
    parameters:
      nomeApp: trilhadeaprendizagem
      namespace: esesp-trilhas
      ambiente: hom
      library: esesp-trilhas-hom
      repoGitops: esesp-trilhadeaprendizagem-gitops
      caminhoAppSettings: Trilhas